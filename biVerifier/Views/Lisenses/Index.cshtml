@model List<biVerifier.Models.Licenses>

@{
    ViewData["Title"] = "LICENSES";
}

@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.16.9/xlsx.full.min.js"></script>
    <script>
        function exportToExcel() {
            var table = document.getElementById('dataTable');
            var ws = XLSX.utils.table_to_sheet(table);
            var wb = XLSX.utils.book_new();
            XLSX.utils.book_append_sheet(wb, ws, 'Sheet1');
            XLSX.writeFile(wb, 'LICENSES.xlsx');
        }
    </script>
}

<div class="custom-container" style="margin-top: 4rem">
    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <div class="box-body">
                    <div class="">
                        <div class="d-flex align-items-center justify-content-between mb-20">
                            <div>
                                <h5 class="box-title" style="text-transform: uppercase; font-weight: bold;">LICENSES DATA PREVIEW</h5>
                                <p><span style="font-weight: bold">NB</span> To view all the data, export to Excel.</p>
                            </div>
                            <div>
                                <button class="btn btn-primary" onclick="exportToExcel()">
                                    <i class="far fa-file-excel"></i> EXPORT TO EXCEL
                                </button>
                            </div>
                        </div>
                    </div>
                    <div class="mb-3">
                        <div class="mb-3">
                            @* <form method="get" asp-controller="Licenses" asp-action="Search">
                                <div class="input-group">
                                    <input type="text" class="form-control" name="searchTerm" placeholder="Search by Client, Requestor, Change Date, Change Code, Current AI, New AI, Licenses No, Cost, or Change Notes" value="@ViewBag.SearchTerm" />
                                    <button class="btn btn-primary" type="submit">Search</button>
                                </div>
                            </form> *@
                        </div>
                    </div>
                    <div class="table-responsive rounded card-table">
                        <table id="dataTable" class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Client</th>
                                    <th>Requestor</th>
                                    <th>Change Date</th>
                                    <th>Change Code</th>
                                    <th>Current AI</th>
                                    <th>New AI</th>
                                    <th>Licenses No</th>
                                    <th>Cost</th>
                                    <th>Change Notes</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var data in Model)
                                {
                                    <tr class="hover-primary">
                                        <td>@data?.Client</td>
                                        <td>@data?.Requestor</td>
                                        <td>@data?.ChangeDate</td>
                                        <td>@data?.ChangeCode</td>
                                        <td>@data?.CurrentAI</td>
                                        <td>@data?.NewAI</td>
                                        <td>@data?.LicensesNo</td>
                                        <td>@data?.Cost</td>
                                        <td>@data?.ChangeNotes</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
